import 'package:flutter/material.dart';

class PlantAddButton extends StatelessWidget {
  const PlantAddButton({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
